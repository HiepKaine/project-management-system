import { sharedFrontendCommon } from './shared-frontend-common';

describe('sharedFrontendCommon', () => {
  it('should work', () => {
    expect(sharedFrontendCommon()).toEqual('shared-frontend-common');
  });
});
