import { GroupByReadingQuestionPipe } from './group-by-reading-question.pipe';

describe('GroupByReadingQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByReadingQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
